{
  "name": "starary-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "desktop:dev": "powershell -ExecutionPolicy Bypass -File ./scripts/desktop-dev.ps1",
    "desktop:build": "npm run release:windows",
    "release:windows": "powershell -ExecutionPolicy Bypass -File ./scripts/release-windows.ps1"
  }
}
